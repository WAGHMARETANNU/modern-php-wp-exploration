# WordPress Template Hierarchy

Template Hierarchy defines how WordPress decides which theme file to load
for a given request (post, page, archive, etc).

WordPress checks files in a specific order.
The first matching file is loaded.


